version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: careloop-postgres
    environment:
      POSTGRES_USER: careloop
      POSTGRES_PASSWORD: careloop_pass
      POSTGRES_DB: careloop_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: careloop-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
